[package]
name = "smcnn-core"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-python", version = "0.1.*" }

[package.host-dependencies]
hatchling = "==1.26.3"

[package.run-dependencies]

[dependencies]
smcnn = { path = "." }
