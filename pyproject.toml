[project]
dependencies = [
    "genbrain",
    "genbrain_smcnn_core",
    "genbrain_smcnn_bioreal",
    "genbrain_model_3dot",
    "genbrain_utils_genjax"
]
name = "genbrain"
requires-python = "<= 3.12"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
